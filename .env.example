# Port API Configuration

# ============================================================================
# Authentication (Choose ONE method)
# ============================================================================

# Method 1: OAuth Credentials (Recommended)
PORT_CLIENT_ID=your-client-id
PORT_CLIENT_SECRET=your-client-secret

# Method 2: JWT Access Token (Alternative)
# PORT_ACCESS_TOKEN=your-jwt-access-token

# ============================================================================
# API Configuration
# ============================================================================

# Port API Region
# Options: 'eu' (default) or 'us'
PORT_REGION=eu

# Or specify Base URL directly (overrides PORT_REGION)
# PORT_BASE_URL=https://api.getport.io

# EU Instance: https://api.getport.io
# US Instance: https://api.us.getport.io

# ============================================================================
# Optional Settings
# ============================================================================

# Request timeout in milliseconds (default: 30000)
# PORT_TIMEOUT=30000

# Maximum retry attempts (default: 3)
# PORT_MAX_RETRIES=3

# ============================================================================
# Proxy Configuration (Optional)
# ============================================================================

# HTTP Proxy URL
# HTTP_PROXY=http://proxy.example.com:8080

# HTTPS Proxy URL
# HTTPS_PROXY=http://proxy.example.com:8080

# Proxy Authentication (Optional)
# PROXY_AUTH_USERNAME=proxy-user
# PROXY_AUTH_PASSWORD=proxy-password

# NO_PROXY - Comma-separated list of hosts to bypass proxy
# NO_PROXY=localhost,127.0.0.1,.local

# ============================================================================
# Logging Configuration (Optional)
# ============================================================================

# Log level: ERROR, WARN, INFO, DEBUG, TRACE
# PORT_LOG_LEVEL=WARN

# Enable verbose logging (sets log level to DEBUG)
# PORT_VERBOSE=false
